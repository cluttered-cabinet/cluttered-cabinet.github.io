---
import Layout from '../../layouts/Layout.astro';
import { getCollection } from 'astro:content';

const allPosts = await getCollection('posts');
const publishedPosts = allPosts.filter(post => !post.data.draft);

const tagCounts = new Map<string, number>();
publishedPosts.forEach(post => {
  post.data.tags?.forEach(tag => {
    tagCounts.set(tag, (tagCounts.get(tag) || 0) + 1);
  });
});

const sortedTags = Array.from(tagCounts.entries()).sort((a, b) => a[0].localeCompare(b[0]));
---

<Layout title="Tags — Cluttered Cabinet">
  <section class="max-w-[1200px] mx-auto px-6 md:px-10 pt-16 md:pt-24 pb-20">
    <div class="border-b border-slate-dark pb-8 mb-0">
      <p class="mono-label mb-4">Index</p>
      <h1 class="font-serif text-[44px] md:text-[60px] leading-[1.05] tracking-tight2 text-slate-dark">
        Tags
      </h1>
      <p class="mono-label mt-6">{sortedTags.length} total</p>
    </div>

    {sortedTags.length > 0 ? (
      <div>
        {sortedTags.map(([tag, count]) => (
          <a href={`/tags/${tag}`} class="row-link no-underline">
            <div class="flex items-baseline justify-between gap-6">
              <span class="font-sans text-[18px] md:text-[20px] font-semibold text-slate-dark tracking-tight2">
                {tag}
              </span>
              <span class="mono-label">
                {count} {count === 1 ? 'entry' : 'entries'}
              </span>
            </div>
          </a>
        ))}
      </div>
    ) : (
      <div class="py-16">
        <p class="mono-label">No tags yet</p>
      </div>
    )}
  </section>
</Layout>
