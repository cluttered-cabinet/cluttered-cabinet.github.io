---
export interface Props {
  src: string;
  alt: string;
  caption?: string;
  credit?: string;
  figureClass?: string;
  fullWidth?: boolean;
  loading?: 'lazy' | 'eager';
}

const {
  src,
  alt,
  caption,
  credit,
  figureClass = '',
  fullWidth = false,
  loading = 'lazy',
} = Astro.props as Props;
---

<figure class={`image-block ${fullWidth ? 'image-block--full' : ''} ${figureClass}`}>
  <img src={src} alt={alt} loading={loading} decoding="async" />
  {(caption || credit) && (
    <figcaption>
      {caption && <span class="image-caption">{caption}</span>}
      {credit && <span class="image-credit">{credit}</span>}
    </figcaption>
  )}
</figure>

<style>
  .image-block {
    margin: 2.25rem 0;
    text-align: center;
  }

  .image-block img {
    width: 100%;
    max-width: 100%;
    border-radius: 12px;
    border: 1px solid rgba(255, 255, 255, 0.06);
  }

  .image-block--full img {
    width: 100%;
    display: block;
  }

  figcaption {
    margin-top: 0.85rem;
    font-size: 0.8rem;
    color: var(--text-secondary);
    line-height: 1.5;
    font-family: var(--font-mono, 'IBM Plex Mono', monospace);
    display: flex;
    flex-wrap: wrap;
    gap: 0.45rem;
    justify-content: center;
  }

  .image-credit {
    opacity: 0.7;
  }
</style>
